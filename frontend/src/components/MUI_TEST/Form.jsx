import react from 'react';
import {Box, Typography, Paper, Button, Container} from '@mui/material'
import { backgroundBg } from '../../utils/constants';

const Form = () =>{

  return(
     <Box sx={{ bgColor:'000', backgroundImage:`url(${backgroundBg})`, backgroundRepeat:'no-repeat',
      backgroundSize:'cover', height:'500px',}}>
      <Container>
     
      </Container>
     </Box>
  )
}

export default Form